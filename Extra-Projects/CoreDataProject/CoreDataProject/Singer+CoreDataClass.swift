import Foundation
import CoreData

@objc(Singer)
public class Singer: NSManagedObject {/* NSManagedObject contains code to encode(serialize) the data(objects) format into a coredata(sql lite) format or to decode(deserialize, aka reverse of encoding). Hence classes which need to be captured in coredata needs to be a subclass of NSManagedObject.*/
}
